import { Controller } from "@nestjs/common";

@Controller("accounts")
export class AccountsController {
  // Después: endpoints tipo
  // POST /accounts/deposit-current
  // POST /accounts/deposit-savings
}
